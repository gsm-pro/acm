#include<fstream>

int main() {
	int $$$$, $$$, $$, $;
	std::ifstream("input.txt") >> $$$$ >> $$$ >> $$;
	$ = ($$$$ - ('@' / ' ') * $$ - $$$) / ('@' / ' ' + ' ' / ' ');
	std::ofstream("output.txt") << $ << " " << $ + $$ << " " << $ + $$$ + $$;
}